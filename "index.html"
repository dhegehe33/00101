<!<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RTX | أقوى هاكات روبلوكس</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Tajawal', sans-serif;
        }

        body {
            background: #0a0a0a;
            color: #ffffff;
            text-align: center;
            overflow: hidden;
        }

        .container {
            position: relative;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 2;
        }

        h1 {
            font-size: 3.5em;
            color: #f1c40f;
            text-shadow: 0 0 25px #fffb99;
            animation: glow 1.5s infinite alternate;
        }

        p {
            font-size: 1.5em;
            color: #dcdcdc;
            margin-bottom: 20px;
        }

        .button {
            background: linear-gradient(45deg, #f1c40f, #fffb99);
            color: #000;
            padding: 15px 40px;
            font-size: 1.5em;
            border: none;
            cursor: pointer;
            border-radius: 10px;
            transition: 0.3s ease-in-out;
            box-shadow: 0 0 20px #f1c40f;
            text-decoration: none;
        }

        .button:hover {
            background: linear-gradient(45deg, #fffb99, #f1c40f);
            transform: scale(1.1);
            box-shadow: 0 0 35px #f1c40f;
        }

        .animated-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background: radial-gradient(circle, rgba(255, 215, 0, 0.2) 10%, transparent 70%);
            animation: pulse 4s infinite alternate;
            z-index: 1;
        }

        @keyframes pulse {
            from { transform: scale(1); opacity: 0.7; }
            to { transform: scale(1.2); opacity: 0.3; }
        }

        @keyframes glow {
            from { text-shadow: 0 0 20px #fffb99; }
            to { text-shadow: 0 0 35px #f1c40f; }
        }

        footer {
            position: absolute;
            bottom: 10px;
            width: 100%;
            font-size: 1.2em;
            color: #dcdcdc;
        }
    </style>
</head>
<body>

    <div class="animated-bg"></div>

    <div class="container">
        <h1>RTX | أقوى هاكات روبلوكس 🔥</h1>
        <p>أفضل الهاكات والسكربتات لتجربة لعب لا مثيل لها!</p>
        <a class="button" href="https://discord.gg/rtxtop1" target="_blank">🚀 انضم إلى سيرفرنا في ديسكورد</a>
    </div>

    <footer>
        &copy; 2025 RTX | جميع الحقوق محفوظة
    </footer>

</body>
</html>
