<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RTX - هاكات روبلوكس</title>
    <link href="https://fonts.googleapis.com/css2?family=El+Messiri:wght@400;700&display=swap" rel="stylesheet">
    <style>
        @keyframes neonBackground {
            0% { background-position: 0 0; }
            50% { background-position: 100% 100%; }
            100% { background-position: 0 0; }
        }

        body {
            font-family: 'El Messiri', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(45deg, #1a1a1a, #2a2a2a);
            background-size: 300% 300%;
            animation: neonBackground 10s infinite alternate;
            color: #f8f8f8;
            text-align: center;
        }

        header {
            background-color: rgba(20, 20, 20, 0.95);
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.8);
        }

        header h1 {
            font-size: 2.5em;
            color: #fff;
            text-shadow: 0 0 10px #bbb;
            margin: 0;
        }

        .menu-button {
            font-size: 2em;
            background: none;
            border: none;
            color: #f1c40f;
            cursor: pointer;
            padding: 10px;
            transition: transform 0.2s;
        }

        .menu-button:hover {
            transform: scale(1.1);
            color: #ffffff;
        }

        .sidebar {
            position: fixed;
            top: 0;
            left: -280px;
            width: 280px;
            height: 100%;
            background: rgba(40, 40, 40, 0.98);
            box-shadow: 3px 0px 15px rgba(0, 0, 0, 0.7);
            transition: left 0.3s ease;
            padding-top: 60px;
        }

        .sidebar a {
            display: block;
            color: #f8f8f8;
            padding: 15px;
            text-decoration: none;
            font-size: 1.2em;
            border-bottom: 1px solid #555;
            transition: background 0.3s, color 0.3s;
        }

        .sidebar a:hover {
            background: #f1c40f;
            color: #000;
        }

        .close-btn {
            position: absolute;
            top: 10px;
            right: 15px;
            font-size: 1.8em;
            cursor: pointer;
            color: #f1c40f;
        }

        .content {
            padding: 40px 20px;
        }

        .content h2 {
            font-size: 2.5em;
            color: #f1c40f;
            text-shadow: 0 0 10px #bbb;
        }

        .content p {
            font-size: 1.2em;
            color: #dcdcdc;
        }

        .button {
            background: linear-gradient(45deg, #f1c40f, #ffeb99);
            color: #000;
            padding: 15px 35px;
            font-size: 1.4em;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            transition: 0.3s;
            box-shadow: 0 0 15px #f1c40f;
        }

        .button:hover {
            background: linear-gradient(45deg, #ffeb99, #f1c40f);
            transform: scale(1.05);
            box-shadow: 0 0 20px #f1c40f;
        }

        footer {
            background-color: rgba(20, 20, 20, 0.95);
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
            font-size: 1em;
            color: #dcdcdc;
            box-shadow: 0px -4px 10px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>

    <header>
        <h1>RTX - هاكات روبلوكس</h1>
        <button class="menu-button" onclick="openSidebar()">|||</button>
    </header>

    <div class="sidebar" id="sidebar">
        <span class="close-btn" onclick="closeSidebar()">✖</span>
        <a href="#">📱 هاك روبلوكس - iOS</a>
        <a href="#">🤖 هاك روبلوكس - Android</a>
        <a href="#">⚡ سكربتات روبلوكس</a>
        <a href="#">📖 دروس وشروحات</a>
    </div>

    <div class="content">
        <h2>أقوى الهاكات لروبلوكس!</h2>
        <p>اكتشف أحدث الهاكات والسيرفرات الخاصة بلعبة روبلوكس مع RTX!</p>
        <button class="button" onclick="window.open('https://discord.gg/rtxtop1', '_blank')">
            🔗 انضم إلى سيرفرنا في ديسكورد
        </button>
    </div>

    <footer>
        <p>&copy; 2025 RTX | جميع الحقوق محفوظة</p>
    </footer>

    <script>
        function openSidebar() {
            document.getElementById("sidebar").style.left = "0";
        }

        function closeSidebar() {
            document.getElementById("sidebar").style.left = "-280px";
        }
    </script>

</body>
</html>